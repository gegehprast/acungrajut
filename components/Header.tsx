import React from 'react'
import Banner from './Banner'

const Header = () => {
    return <header className="relative w-full h-screen overflow-hidden">
        <Banner />
    </header>
}

export default Header
